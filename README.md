# โรงเรียนเบญจมราชูทิศ - ระบบติดตามผลการรับสมัคร ม.4 ปีการศึกษา 2569

แอปพลิเคชันเว็บสำหรับติดตามการอัปเดตผลการรับสมัครนักเรียนชั้นมัธยมศึกษาปีที่ 4 (ม.4) ปีการศึกษา 2569 ของโรงเรียนเบญจมราชูทิศ

## ฟีเจอร์หลัก

- ✅ **ติดตามการอัปเดตอัตโนมัติ** - ตรวจสอบการเปลี่ยนแปลงในหน้าเว็บตามช่วงเวลาที่กำหนด
- 🔔 **ระบบแจ้งเตือน** - แจ้งเตือนเมื่อพบการอัปเดตใหม่
- 📱 **รองรับมือถือ** - ออกแบบให้ใช้งานได้บนอุปกรณ์ทุกขนาด
- 💾 **บันทึกประวัติ** - เก็บประวัติการแจ้งเตือนไว้ตรวจสอบย้อนหลัง
- ⚡ **ตรวจสอบทันที** - สามารถตรวจสอบการอัปเดตได้ทันที

## การใช้งาน

### การเปิดใช้งานแบบท้องถิ่น (Local)

เปิดไฟล์ `index.html` ในเว็บเบราว์เซอร์เพื่อใช้งาน

### การ Deploy บน Vercel

1. **เตรียมไฟล์สำหรับ Deploy**
   - ไฟล์หลัก: `index.html` (รวม HTML, CSS, และ JavaScript แล้ว)
   - ไฟล์กำหนดค่า: `vercel.json`

2. **Deploy บน Vercel**
   ```bash
   # ติดตั้ง Vercel CLI (หากยังไม่มี)
   npm install -g vercel

   # Deploy
   vercel

   # หรือ Deploy โดยตรงจาก GitHub
   # สร้าง Repository ใหม่บน GitHub และ Push โค้ดทั้งหมด
   # จากนั้น Connect Repository เข้ากับ Vercel
   ```

3. **การตั้งค่า Vercel**
   - **Build Command**: ไม่ต้องกำหนด (Static Site)
   - **Output Directory**: `./`
   - **Root Directory**: `./`

4. **URL ที่ได้หลัง Deploy**
   - Vercel จะสร้าง URL อัตโนมัติ เช่น: `https://your-project-name.vercel.app`

## การแก้ไขปัญหา

### ปัญหา 404 NOT_FOUND
- ตรวจสอบว่าใช้ไฟล์ `index.html` เป็นไฟล์หลัก
- ตรวจสอบไฟล์ `vercel.json` มีการกำหนด Routes ถูกต้อง

### ปัญหา CORS (Cross-Origin Resource Sharing)
- แอปพลิเคชันใช้ Proxy Service เพื่อดึงข้อมูลจากเว็บโรงเรียน
- หากมีปัญหา CORS จะแสดงข้อมูลตัวอย่างแทน

## เทคโนโลยีที่ใช้

- **HTML5** - โครงสร้างหน้าเว็บ
- **Tailwind CSS** - สไตล์และการออกแบบที่ตอบโจทย์
- **JavaScript (ES6+)** - ฟังก์ชันการทำงาน
- **Font Awesome** - ไอคอน
- **Web APIs** - Local Storage, Notifications

## การตั้งค่า

### การเรียกใช้งานแบบ Local

1. ดาวน์โหลดหรือโคลนโปรเจคนี้
2. เปิดไฟล์ `index.html` ในเว็บเบราว์เซอร์
3. อนุญาตการแจ้งเตือนเมื่อเบราว์เซอร์ขออนุญาต

### การตั้งค่าเซิร์ฟเวอร์ (ถ้าต้องการ)

เนื่องจากการจำกัด CORS อาจต้องใช้เซิร์ฟเวอร์ local:

```bash
# ใช้ Python
python -m http.server 8000

# หรือใช้ Node.js
npx serve .

# จากนั้นเข้าผ่าน http://localhost:8000
```

## ข้อจำกัดและวิธีแก้ไข

### ปัญหา CORS

เนื่องจากนโยบาย CORS ของเบราว์เซอร์ การดึงข้อมูลโดยตรงจากเว็บไซต์อาจถูกบล็อก แอปพลิเคชันนี้ใช้:

1. **Proxy Service** - ใช้บริการพร็อกซีเพื่อดึงข้อมูล
2. **Fallback Data** - มีข้อมูลตัวอย่างสำหรับการทดสอบ

### วิธีแก้ไขปัญหา CORS

1. **ใช้เซิร์ฟเวอร์ Local** - ติดตั้งเซิร์ฟเวอร์ local
2. **ปิดการตรวจสอบ CORS** (สำหรับ Chrome):
   ```bash
   chrome.exe --disable-web-security --user-data-dir="C:/temp/chrome_dev"
   ```

## การพัฒนาต่อ

### ฟีเจอร์ที่สามารถเพิ่มเติม

- [ ] การส่งอีเมลแจ้งเตือน
- [ ] การเชื่อมต่อกับ LINE Notify
- [ ] การกรองเนื้อหาที่ต้องการติดตามโดยเฉพาะ
- [ ] การส่งข้อมูลไปยัง Discord/Slack
- [ ] การแสดงกราฟสถิติการอัปเดต

### การแก้ไขโค้ด

- แก้ไข `script.js` สำหรับฟังก์ชันการทำงาน
- แก้ไข `index.html` สำหรับการออกแบบหน้าเว็บ
- ปรับแต่ง CSS ผ่าน Tailwind CSS

## ข้อมูลติดต่อ

- **โรงเรียนเบญจมราชูทิศ**: https://www.benjama.ac.th/
- **หน้ารับสมัคร**: https://www.benjama.ac.th/admission/admission-69/

## ใบอนุญาต

โปรเจคนี้พัฒนาขึ้นเพื่อการศึกษาและใช้งานส่วนตัว

---

**หมายเหตุ**: แอปพลิเคชันนี้ไม่ได้รับการสนับสนุนจากโรงเรียนเบญจมราชูทิศ และพัฒนาขึ้นเพื่อความสะดวกในการติดตามข้อมูลเท่านั้น
